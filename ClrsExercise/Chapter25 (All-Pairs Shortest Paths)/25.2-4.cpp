// 丢弃上标之后, d_ij = min(d_ij, d_ik + d_kj).
// 与之前的区别为, 之前使用的是min(d_k-1_ij, d_k-1_ik + d_k-1_kj).
// 而丢弃之后, 会出现d_k_ik + d_k-1_kj, 或者d_k-1_ik + d_k_kj等等情况.
// 但是, 在d_k_ik或者d_k_kj中, 不会包含k顶点, 否则会产生环.
// 因此, d_k_ik = d_k-1_ik, d_k_kj = d_k-1_kj.
// 而d_k_ij在循环改变之前, 等于d_k-1_ij, 而修改之后, 会替换为更小的值, 
// 而且依然不包含k(上面已经证明).
// 因此, 去掉上标依然是正确的.
//
