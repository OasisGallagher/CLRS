/**
	此时m = 2^p - 1, 也就是二进制的p个1.
	对于串: abcdef ...(共n位).
	通过x = 2^p为基数进行解释:
	(((a * x + b) * x + c) * x) + d ...
	也就是:
	Y = a * x^(n - 1) + b * x^(n - 2) + c * x^(n - 3) + ...
	Y % m = (a * x^(n - 1) % m + b * x^(n - 2) % m + c * x^(n - 3) + ...) % m
	      = (a * x^(n - 2) * x % m ...
		  = (a * x^(n - 2) * (1 + m) % m ...
		  = (0 + a * x^(n - 2) % m ...
		  ...
		  = a * x % m ...
		  = a * (1 + m) % m ...
		  = (a + m) % m
		  = (a % m + b % m + c % m ...) % m
		  = (a + b + c ...) % m
	
	与排列顺序无关, 只要是由相同的元素组成的字符串, hash值相等.
	
	也就是说, (解释基数 = 取模的除数 + 1)是不合理的hash函数.
*/
